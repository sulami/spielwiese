/* This tests the binary search tree class */
#include <stdio.h>
#include "binarytree.h"

int main(int argc, const char *argv[])
{
    char action;
    while (action != 'e') {
        scanf("%c", &action);
    }
    return 0;
}

